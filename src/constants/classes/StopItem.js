export class StopItem {
    constructor(place = '', arrivalTime = '', departureTime = '') {
        ;(this.place = place),
            (this.arrivalTime = arrivalTime),
            (this.departureTime = departureTime)
    }
}
